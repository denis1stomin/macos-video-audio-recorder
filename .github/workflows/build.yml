name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v5

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build
        run: |
          xcodebuild -project RexGrab.xcodeproj -scheme RexGrab -configuration Debug \
            -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build

      - name: Run unit tests
        run: |
          xcodebuild -project RexGrab.xcodeproj -scheme RexGrab -configuration Debug \
            -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test
